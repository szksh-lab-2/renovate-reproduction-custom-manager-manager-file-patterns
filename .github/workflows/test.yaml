---
name: test
on: push
jobs:
  test:
    runs-on: ubuntu-24.04
    permissions:
      contents: write
      issues: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: true
      - run: npx --yes --package "renovate" -c "renovate-config-validator --strict renovate.json5"
